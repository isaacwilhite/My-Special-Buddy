import { Carousel } from 'react-bootstrap';

const Gallery = () => {
    return (
        <Carousel>
            {/* Add Carousel.Item for each image */}
        </Carousel>
    );
};

export default Gallery